<DataTemplate xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:vs="http://schemas.microsoft.com/visualstudio/extensibility/2022/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <vs:ListView ItemsSource="{Binding Messages}" Grid.Row="0">
            <vs:ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock>
                        <Run Text="{Binding Author}" />
                        <Run Text=": " />
                        <Run Text="{Binding Text}" />
                    </TextBlock>
                </DataTemplate>
            </vs:ListView.ItemTemplate>
        </vs:ListView>
        <TextBox Text="{Binding Input}" Grid.Row="1" />
        <Button Content="Send" Command="{Binding SendCommand}" Grid.Row="2" />
    </Grid>
</DataTemplate>