<UserControl x:Class="KrnlAI.VisualStudio.ToolWindows.KrnlAIToolWindowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="350"
             Background="{DynamicResource VsBrush.Window}">
  <Grid Margin="8">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0" Text="Krnl-AI" FontSize="16" FontWeight="Bold"
               Foreground="{DynamicResource VsBrush.CaptionText}" Margin="0,0,0,8" />

    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
      <Button x:Name="ConnectButton" Content="Connect" Click="OnConnect" Width="80" Height="24" Margin="0,0,8,0" />
      <TextBlock x:Name="StatusText" Text="Disconnected" VerticalAlignment="Center"
                 Foreground="{DynamicResource VsBrush.GrayText}" />
      <TextBlock x:Name="MoodText" Text="" VerticalAlignment="Center"
                 Foreground="{DynamicResource VsBrush.CaptionText}" Margin="8,0,0,0" FontWeight="SemiBold" />
    </StackPanel>

    <TabControl Grid.Row="2" Margin="0,0,0,8">
      <TabItem Header="Chat">
        <Grid>
          <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
          </Grid.RowDefinitions>
          <ListBox x:Name="ChatList" BorderThickness="0"
                   Foreground="{DynamicResource VsBrush.WindowText}" />
          <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
              </Grid.RowDefinitions>
              <TextBox x:Name="ChatInput" Grid.Row="0" Height="24" Margin="0,4,4,0" TextChanged="OnChatInputTextChanged" />
              <ListBox x:Name="CommandSuggestions" Grid.Row="1" Height="60" Margin="0,0,4,0"
                       Visibility="Collapsed" SelectionChanged="OnCommandSuggestionSelected"
                       FontSize="11" Background="{DynamicResource VsBrush.ToolWindowBackground}" />
            </Grid>
            <Button x:Name="SendButton" Grid.Column="1" Content="Send" Width="60" Height="24" Margin="0,4,0,0" Click="OnSend" />
          </Grid>
        </Grid>
      </TabItem>
      <TabItem Header="Memory">
        <Grid>
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
          </Grid.RowDefinitions>
          <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="MemorySearchInput" Grid.Column="0" Height="24" />
            <Button x:Name="SearchButton" Grid.Column="1" Content="Search" Width="60" Height="24" Margin="4,0,0,0" Click="OnSearch" />
          </Grid>
          <ListBox x:Name="MemoryList" Grid.Row="1" BorderThickness="0"
                   Foreground="{DynamicResource VsBrush.WindowText}" />
        </Grid>
      </TabItem>
      <TabItem Header="Status">
        <StackPanel>
          <TextBlock x:Name="EndpointText" Margin="0,0,0,4" />
          <TextBlock x:Name="HealthText" Margin="0,0,0,4" />
          <TextBlock x:Name="StatusMoodText" Text="" Margin="0,0,0,4" FontWeight="SemiBold" />
          <Button x:Name="RefreshButton" Content="Refresh" Width="80" Height="24" Click="OnRefresh" />
        </StackPanel>
      </TabItem>
    </TabControl>

    <TextBlock x:Name="ErrorText" Grid.Row="3" Foreground="{DynamicResource VsBrush.CommandBarTextActive}"
               TextWrapping="Wrap" />
  </Grid>
</UserControl>
