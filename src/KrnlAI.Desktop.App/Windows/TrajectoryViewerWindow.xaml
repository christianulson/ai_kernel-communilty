<Window x:Class="KrnlAI.Desktop.App.Windows.TrajectoryViewerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Trajectory Viewer" Height="600" Width="900"
        WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,1,0">
            <Grid Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBox Grid.Row="0" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,0,0,8" Padding="6" FontSize="12">
                    <TextBox.InputBindings>
                        <KeyBinding Key="Return" Command="{Binding RefreshCommand}"/>
                    </TextBox.InputBindings>
                </TextBox>
                <ListBox Grid.Row="1" x:Name="SessionList" ItemsSource="{Binding Sessions}"
                         DisplayMemberPath="DisplayText"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         MouseDoubleClick="OnSessionDoubleClick"/>
                <TextBlock Grid.Row="1" Text="No sessions"
                           HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.4" IsHitTestVisible="False"/>
            </Grid>
        </Border>

        <!-- Main -->
        <Grid Grid.Column="1" Margin="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                <Button Command="{Binding RefreshCommand}" Content="⟳ Refresh" Padding="8,4" Margin="0,0,8,0"/>
                <TextBlock Text="{Binding SelectedSession.Prompt}" FontSize="13" FontWeight="SemiBold"
                           VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
            </StackPanel>

            <TabControl Grid.Row="1" SelectedIndex="{Binding SelectedTabIndex}">
                <TabItem Header="Steps">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding SelectedSession.Steps}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Margin="0,2" Padding="8" CornerRadius="4" BorderThickness="1"
                                            BorderBrush="{StaticResource BorderBrush}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="{Binding Sequence, StringFormat='#{0}'}"
                                                       FontSize="11" Opacity="0.5" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                            <StackPanel Grid.Column="1">
                                                <TextBlock Text="{Binding Action}" FontWeight="SemiBold" FontSize="12"/>
                                                <TextBlock Text="{Binding Stage}" FontSize="10" Opacity="0.6"/>
                                                <TextBlock Text="{Binding Error}" FontSize="11" Foreground="Red"
                                                           Visibility="{Binding Error, Converter={StaticResource StringToVisibilityConverter}}"/>
                                            </StackPanel>
                                            <StackPanel Grid.Column="2" HorizontalAlignment="Right">
                                                <TextBlock Text="{Binding DurationMs, StringFormat={}{0}ms}" FontSize="11"/>
                                                <TextBlock Text="{Binding TokensUsed, StringFormat={}{0} tokens}" FontSize="10" Opacity="0.5"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </TabItem>
                <TabItem Header="Events">
                    <Grid>
                        <TextBlock Text="Event table view (sorted by sequence)" Margin="8"
                                   HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.4"/>
                    </Grid>
                </TabItem>
                <TabItem Header="Time">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding SelectedSession.Steps}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,2">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="80"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="60"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding Action}" FontSize="11"
                                                   TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                                        <Border Grid.Column="1" Height="16" CornerRadius="3"
                                                Background="{StaticResource AccentBrush}"
                                                HorizontalAlignment="Left"
                                                Width="100"/>
                                        <TextBlock Grid.Column="2" Text="{Binding DurationMs, StringFormat={}{0}ms}"
                                                   FontSize="10" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </TabItem>
            </TabControl>
        </Grid>
    </Grid>
</Window>
