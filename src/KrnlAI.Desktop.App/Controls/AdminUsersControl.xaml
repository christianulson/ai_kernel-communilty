<UserControl x:Class="KrnlAI.Desktop.App.Controls.AdminUsersControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="400" d:DesignWidth="600">
  <Grid Margin="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <TextBlock Grid.Row="0" Text="Admin — Users" FontSize="18" FontWeight="Bold" Margin="0,0,0,12" />
    <DataGrid x:Name="UsersGrid" Grid.Row="1" IsReadOnly="True" AutoGenerateColumns="False"
              SelectionChanged="OnUserSelected" Margin="0,0,0,8">
      <DataGrid.Columns>
        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" />
        <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*" />
        <DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="100" />
        <DataGridTextColumn Header="Active" Binding="{Binding IsActive}" Width="80" />
      </DataGrid.Columns>
    </DataGrid>
    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
      <Button x:Name="ActivateButton" Content="Activate" Width="80" Height="28" Margin="0,0,8,0" Click="OnActivate" IsEnabled="False" />
      <Button x:Name="SuspendButton" Content="Suspend" Width="80" Height="28" Click="OnSuspend" IsEnabled="False" />
    </StackPanel>
  </Grid>
</UserControl>
