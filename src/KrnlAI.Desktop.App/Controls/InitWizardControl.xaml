<UserControl x:Class="KrnlAI.Desktop.App.Controls.InitWizardControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Loaded="OnLoaded">
    <ScrollViewer Padding="28" VerticalScrollBarVisibility="Auto">
        <StackPanel MaxWidth="600">
            <TextBlock Text="🚀 Novo Projeto" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
            <TextBlock Text="Crie um novo projeto KrnlAI." FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,4,0,18"/>

            <Border Background="{DynamicResource PanelBrush}" CornerRadius="24" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="24">
                <StackPanel>
                    <TextBlock Text="Nome do Projeto" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                    <TextBox Text="{Binding InitWizardVM.ProjectName, UpdateSourceTrigger=PropertyChanged}" Height="36" FontSize="14" Margin="0,0,0,12"/>

                    <TextBlock Text="Localização" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                        <TextBox Text="{Binding InitWizardVM.Location}" Height="36" FontSize="13" IsReadOnly="True"/>
                        <Button Grid.Column="1" Command="{Binding InitWizardVM.BrowseCommand}" Height="36" Width="80" Margin="8,0,0,0" Content="..."/>
                    </Grid>

                    <TextBlock Text="Template" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                    <ComboBox ItemsSource="{Binding InitWizardVM.Templates}" SelectedItem="{Binding InitWizardVM.SelectedTemplate}" Height="36" Margin="0,0,0,16"/>

                    <Button Command="{Binding InitWizardVM.CreateCommand}" Style="{StaticResource PrimaryButton}" Height="40" Content="Criar Projeto"/>
                    <TextBlock Text="{Binding InitWizardVM.Status}" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
